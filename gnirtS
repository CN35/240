public class Kata {

  public static String solution(String str) {
    // Your code here...
    String rts = "";
    for (int i = 1; i < str.length()+1; i++){rts = rts.concat(String.valueOf(str.charAt(str.length()-i)));}
    return rts;
  }

}
